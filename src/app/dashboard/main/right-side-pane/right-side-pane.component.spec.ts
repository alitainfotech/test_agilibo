import { RightSidePaneComponent } from './right-side-pane.component';

describe('RightSidePaneComponent', () => {
  let component: RightSidePaneComponent;

  beforeEach(() => {
    component = new RightSidePaneComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
