import { LeftSidePaneComponent } from './left-side-pane.component';

describe('LeftSidePaneComponent', () => {
  let component: LeftSidePaneComponent;

  beforeEach(() => {
    component = new LeftSidePaneComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
